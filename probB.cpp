#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    long long int test;
    cin >> test;
    while (test--)
    {
        long long int x;
        cin >> x;
        int arr[x+5];
        for (int i = 0; i < x; i++)
        {
            /* code */
            cin >> arr[i];
            
        }
        
    }
}